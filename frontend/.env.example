# API base URL — include /api suffix
# Local dev: leave unset (Vite proxies /api → http://localhost:5001)
# VITE_API_URL=

# Production on Vercel — set to your Render backend URL in Vercel dashboard
# VITE_API_URL=https://nutrifit-api.onrender.com/api
